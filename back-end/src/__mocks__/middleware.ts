export const loadPlayer = jest.fn((req, res, next) => next());
export const joinPhaseHandler = jest.fn((req, res, next) => next());
export const loadNames = jest.fn((req, res, next) => next());
export const loadStory = jest.fn((req, res, next) => next());
