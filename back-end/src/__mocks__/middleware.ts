export const loadUser = jest.fn((req, res, next) => next());
export const joinPhase = jest.fn((req, res, next) => next());
export const loadNames = jest.fn((req, res, next) => next());
export const loadStory = jest.fn((req, res, next) => next());
export const accessLogger = jest.fn((req, res, next) => next());
